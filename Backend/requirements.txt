fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy==2.0.36
greenlet==3.1.1
asyncpg==0.30.0
alembic==1.13.3
pydantic==2.10.4
pydantic-settings==2.6.1
email-validator==2.2.0
pyjwt==2.10.1
python-multipart==0.0.22

# Pinned together: bcrypt>=4.1 removed the `__about__` attribute passlib<=1.7.4
# probes for, which raises at import/hash time. This pair is the known-good combo.
passlib==1.7.4
bcrypt==4.0.1

python-dotenv==1.1.1

# Brevo transactional email HTTP API (app/services/email_service.py)
httpx==0.28.1
# Proposal/invoice PDF generation (app/services/pdf_service.py) - pure
# Python, no system binary/library dependency, safe for the existing
# Render deployment.
fpdf2==2.8.2

# Compression - Brotli for response compression (pure Python fallback available)
brotli==1.1.0

# Faster JSON serialization (uvicorn detects and uses automatically)
orjson==3.10.15

# Prometheus metrics (A09: /metrics endpoint + request instrumentation)
prometheus-fastapi-instrumentator==7.1.0

# Error tracking (env-gated: only active when SENTRY_DSN is set)
sentry-sdk[fastapi]==2.24.1
