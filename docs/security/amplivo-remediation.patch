diff --git a/Backend/app/modules/file_manager/routes.py b/Backend/app/modules/file_manager/routes.py
--- a/Backend/app/modules/file_manager/routes.py
+++ b/Backend/app/modules/file_manager/routes.py
@@ -23,8 +23,26 @@ router = APIRouter(prefix="/files", tags=["File Manager"])
 UPLOADS_DIR = Path(__file__).resolve().parents[3] / "uploads"
 UPLOADS_DIR.mkdir(exist_ok=True)
 MAX_UPLOAD_SIZE = 20 * 1024 * 1024  # 20MB
 
+# SECURITY-1 (CRITICAL)  -  Upload allowlist. Active-content browsers+server
+# (HTML/SVG/JS...) would be served back as text/html from /uploads and execute
+# as stored XSS in any visitor's browser (reproduced on production). Both the
+# file EXTENSION and the declared MIME type must match before a byte is saved.
+ALLOWED_UPLOAD_EXTENSIONS = {
+    ".pdf", ".png", ".jpg", ".jpeg", ".gif", ".webp",
+    ".doc", ".docx", ".xls", ".xlsx", ".csv", ".txt",
+}
+ALLOWED_UPLOAD_MIME_TYPES = {
+    "application/pdf",
+    "image/png", "image/jpeg", "image/gif", "image/webp",
+    "application/msword",
+    "application/vnd.ms-excel",
+    "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
+    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
+    "text/csv", "text/plain",
+}
+
 
 @router.post("/upload", response_model=FileRead, status_code=status.HTTP_201_CREATED, summary="Upload a file")
 async def upload_file(
     request: Request,
@@ -37,9 +55,9 @@ async def upload_file(
     scoped_client_id: uuid.UUID | None = Depends(get_current_client_id),
 ):
     if folder_id is not None:
         # Raises NotFoundException (404) / ForbiddenException (403) if the
-        # folder doesn't exist or belongs to another client — otherwise this
+        # folder doesn't exist or belongs to another client  -  otherwise this
         # would only surface later as a foreign-key IntegrityError (500).
         await folder_service.get(folder_id, scoped_client_id=scoped_client_id)
 
     contents = await upload.read()
@@ -48,9 +66,16 @@ async def upload_file(
     if len(contents) > MAX_UPLOAD_SIZE:
         raise BadRequestException("File exceeds the 20MB upload limit.")
 
     original_name = upload.filename or "file"
-    safe_suffix = Path(original_name).suffix
+    safe_suffix = Path(original_name).suffix.lower()
+    content_type = (upload.content_type or "").lower()
+
+    if safe_suffix not in ALLOWED_UPLOAD_EXTENSIONS:
+        raise BadRequestException(f"File type '{safe_suffix or '(none)'}' is not allowed.")
+    if content_type not in ALLOWED_UPLOAD_MIME_TYPES:
+        raise BadRequestException(f"File content type '{content_type}' is not allowed.")
+
     stored_name = f"{uuid.uuid4().hex}{safe_suffix}"
     (UPLOADS_DIR / stored_name).write_bytes(contents)
 
     # FileCreate.url is validated as an absolute http(s) URL, so the stored
@@ -61,9 +86,9 @@ async def upload_file(
 
     file_obj = await service.create(FileCreate(
         name=stored_name,
         original_name=original_name,
-        mime_type=upload.content_type,
+        mime_type=content_type,
         size=len(contents),
         url=file_url,
         folder_id=folder_id,
         client_id=scoped_client_id,
@@ -140,5 +165,5 @@ async def create_file(data: FileCreate, db: AsyncSession = Depends(get_db), serv
 
 @router.delete("/{id}", status_code=status.HTTP_204_NO_CONTENT)
 async def delete_file(id: uuid.UUID, db: AsyncSession = Depends(get_db), service: FileService = Depends(get_file_service), _: User = Depends(get_current_user), scoped_client_id: uuid.UUID | None = Depends(get_current_client_id)):
     await service.delete(id, scoped_client_id=scoped_client_id)
-    await db.commit()
+    await db.commit()
\ No newline at end of file

diff --git a/Backend/app/modules/file_manager/service.py b/Backend/app/modules/file_manager/service.py
--- a/Backend/app/modules/file_manager/service.py
+++ b/Backend/app/modules/file_manager/service.py
@@ -1,8 +1,10 @@
 """Service for the File Manager module."""
 from __future__ import annotations
 
+import logging
 import uuid
+from pathlib import Path
 
 from sqlalchemy.ext.asyncio import AsyncSession
 
 from app.modules.file_manager.models import FileFolder, File
@@ -10,8 +12,12 @@ from app.modules.file_manager.repository import FileFolderRepository, FileReposi
 from app.modules.file_manager.schemas import FileFolderCreate, FileCreate
 from app.core.exceptions import NotFoundException
 from app.core.tenant_scope import enforce_client_scope
 
+logger = logging.getLogger(__name__)
+
+UPLOADS_DIR = Path(__file__).resolve().parents[3] / "uploads"
+
 
 class FileFolderService:
     def __init__(self, session: AsyncSession) -> None:
         self._session = session
@@ -53,6 +59,15 @@ class FileService:
     async def create(self, data: FileCreate) -> File:
         return await self._repo.create_from_dict(data.model_dump())
 
     async def delete(self, id: uuid.UUID, *, scoped_client_id: uuid.UUID | None = None) -> None:
-        await self.get(id, scoped_client_id=scoped_client_id)
+        file_obj = await self.get(id, scoped_client_id=scoped_client_id)
         await self._repo.delete(id)
+        # SECURITY-2 (CRITICAL, BAC-2): also unlink the physical file, not just
+        # the metadata row. Previously the row was deleted but /uploads/<name>
+        # kept serving the payload forever (stored XSS survived "deletion").
+        stored_name = getattr(file_obj, "name", None)
+        if stored_name:
+            try:
+                (UPLOADS_DIR / stored_name).unlink(missing_ok=True)
+            except OSError as exc:
+                logger.warning("Failed to unlink uploaded file %s: %s", stored_name, exc)
\ No newline at end of file

diff --git a/Backend/app/main.py b/Backend/app/main.py
--- a/Backend/app/main.py
+++ b/Backend/app/main.py
@@ -32,14 +32,24 @@ from app.middleware.security_headers import SecurityHeadersMiddleware
 from app.middleware.session import SessionMiddleware
 
 logger = logging.getLogger("app.startup")
 
+IS_PRODUCTION = settings.ENVIRONMENT.lower() == "production"
+
 
 @asynccontextmanager
 async def lifespan(app: FastAPI):
     # ── Structured logging ────────────────────────────────────────────────
     setup_logging(level=settings.LOG_LEVEL)
 
+    # ── Fail-closed guard against shipped default secrets ──────────────────
+    # SECURITY-2: never boot production with the placeholder JWT secret.
+    if IS_PRODUCTION and settings.JWT_SECRET_KEY == "CHANGE_ME_IN_PRODUCTION":
+        raise RuntimeError(
+            "Refusing to start in production: JWT_SECRET_KEY is still the placeholder. "
+            "Set JWT_SECRET_KEY via env/secrets before deploying."
+        )
+
     # ── Database connectivity ─────────────────────────────────────────────
     is_healthy, latency_ms = await check_database_connection()
     if is_healthy:
         logger.info(
@@ -50,9 +60,9 @@ async def lifespan(app: FastAPI):
         # blocks the application from starting up to serve requests.
         asyncio.create_task(_seed_demo_background())
     else:
         logger.warning(
-            "Database connection could not be verified at startup — "
+            "Database connection could not be verified at startup  -  "
             "the app will still boot; check DATABASE_URL and DB_SSL_MODE."
         )
     yield
     await engine.dispose()
@@ -68,9 +78,9 @@ async def _seed_demo_background() -> None:
             created = await seed_demo_data(session)
         if any(created.values()):
             logger.info("Demo data seeded", extra={"created": created})
     except Exception:
-        logger.exception("Demo data seeding failed — continuing without it.")
+        logger.exception("Demo data seeding failed  -  continuing without it.")
 
 
 app = FastAPI(
     title=settings.PROJECT_NAME,
@@ -80,11 +90,15 @@ app = FastAPI(
         "Portal: registration, login, logout, token refresh, current-user "
         "retrieval, and Phase 2 enterprise security (audit logging, login "
         "history, account lockout, device tracking, rate limiting)."
     ),
-    openapi_url=f"{settings.API_V1_PREFIX}/openapi.json",
-    docs_url=f"{settings.API_V1_PREFIX}/docs",
-    redoc_url=f"{settings.API_V1_PREFIX}/redoc",
+    # SECURITY-3 (MED-1): hide /docs + OpenAPI schema in production to stop
+    # unauthenticated visitors enumerating the full 552KB API surface.
+    openapi_url=(
+        f"{settings.API_V1_PREFIX}/openapi.json" if not IS_PRODUCTION else None
+    ),
+    docs_url=(f"{settings.API_V1_PREFIX}/docs" if not IS_PRODUCTION else None),
+    redoc_url=(f"{settings.API_V1_PREFIX}/redoc" if not IS_PRODUCTION else None),
     lifespan=lifespan,
 )
 
 # Middleware is added innermost-first: Starlette wraps the stack so the LAST
@@ -134,14 +148,19 @@ app.include_router(api_router, prefix=settings.API_V1_PREFIX)
 
 
 @app.get("/", include_in_schema=False)
 async def root():
+    if IS_PRODUCTION:
+        return JSONResponse(
+            status_code=200,
+            content={"status": "ok", "docs": "disabled in production"},
+        )
     return RedirectResponse(url=f"{settings.API_V1_PREFIX}/docs")
 
 
 @app.get("/health", tags=["Health"], summary="Liveness probe")
 async def liveness() -> dict[str, str]:
-    """Minimal liveness check — never touches the database."""
+    """Minimal liveness check  -  never touches the database."""
     return {"status": "ok"}
 
 
 @app.get("/health/ready", tags=["Health"], summary="Readiness probe")
@@ -185,5 +204,5 @@ async def _db_health(db: AsyncSession) -> JSONResponse:
             "status": "healthy",
             "database": "connected",
             "latency_ms": round(latency_ms, 2),
         },
-    )
+    )
\ No newline at end of file

diff --git a/Backend/app/core/config.py b/Backend/app/core/config.py
--- a/Backend/app/core/config.py
+++ b/Backend/app/core/config.py
@@ -11,14 +11,14 @@ class Settings(BaseSettings):
     PROJECT_NAME: str = "Amplivo ERP Auth Service"
     API_V1_PREFIX: str = "/api/v1"
     ENVIRONMENT: str = "development"
 
-    # The single connection string SQLAlchemy/asyncpg actually connects
-    # with. Everything else DB-related below configures how that connection
-    # behaves (pooling, SSL) - it does not change which database is used.
-    DATABASE_URL: str = Field(
-        default="postgresql+asyncpg://postgres.fhxkiprlcdwbgtaxlffk:REDACTED@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres"
-    )
+    # SECURITY (CRITICAL-2): the connection string CANNOT be baked into the
+    # source default anymore  -  the old default embedded the live Supabase
+    # password (public in git history). The deployment now MUST supply
+    # DATABASE_URL via environment/secrets; an empty default fails fast the
+    # same way a missing env var would in production.
+    DATABASE_URL: str = Field(default="")
 
     DB_POOL_SIZE: int = 5
     DB_MAX_OVERFLOW: int = 10
     DB_POOL_RECYCLE_SECONDS: int = 1800
@@ -38,9 +38,9 @@ class Settings(BaseSettings):
     SUPABASE_URL: str | None = None
     SUPABASE_ANON_KEY: str | None = None
     SUPABASE_SERVICE_ROLE_KEY: str | None = None
 
-    JWT_SECRET_KEY: str = Field(default="CHANGE_ME_IN_PRODUCTION")
+    JWT_SECRET_KEY: str = Field(default="")
     JWT_ALGORITHM: str = "HS256"
     ACCESS_TOKEN_EXPIRE_MINUTES: int = 30
     REFRESH_TOKEN_EXPIRE_DAYS: int = 7
 
@@ -126,5 +126,5 @@ class Settings(BaseSettings):
 def get_settings() -> Settings:
     return Settings()
 
 
-settings = get_settings()
+settings = get_settings()
\ No newline at end of file

diff --git a/Backend/app/modules/activity_timeline/routes.py b/Backend/app/modules/activity_timeline/routes.py
--- a/Backend/app/modules/activity_timeline/routes.py
+++ b/Backend/app/modules/activity_timeline/routes.py
@@ -5,11 +5,13 @@ import uuid
 
 from fastapi import APIRouter, Depends, status
 from sqlalchemy.ext.asyncio import AsyncSession
 
+from app.core.exceptions import ForbiddenException
 from app.dependencies.auth import get_current_user
 from app.dependencies.db import get_db
 from app.dependencies.rbac import require_roles
+from app.dependencies.tenant import get_current_client_id
 from app.models.user import User
 from app.modules.activity_timeline.dependencies import get_activity_log_service
 from app.modules.activity_timeline.schemas import ActivityLogCreate, ActivityLogRead
 from app.modules.activity_timeline.service import ActivityLogService
@@ -20,16 +22,37 @@ router = APIRouter(prefix="/activity-logs", tags=["Activity Timeline"])
 @router.get("", response_model=list[ActivityLogRead])
 async def list_logs(
     skip: int = 0, limit: int = 100,
     entity_type: str | None = None, entity_id: uuid.UUID | None = None,
-    service: ActivityLogService = Depends(get_activity_log_service), _: User = Depends(get_current_user),
+    service: ActivityLogService = Depends(get_activity_log_service),
+    current_user: User = Depends(get_current_user),
+    scoped_client_id: uuid.UUID | None = Depends(get_current_client_id),
 ):
+    # SECURITY-4 (BAC-1): a client-scoped caller may ONLY ever see activity
+    # logs for their own user. Previously any portal (client) credential could
+    # read the global audit trail (100 rows across 4 other users' operations).
+    if scoped_client_id is not None:
+        return await service.list_all(
+            skip=skip, limit=limit,
+            entity_type=entity_type, entity_id=entity_id,
+            user_id=current_user.id,
+        )
     return await service.list_all(skip=skip, limit=limit, entity_type=entity_type, entity_id=entity_id)
 
 
 @router.get("/{id}", response_model=ActivityLogRead)
-async def get_log(id: uuid.UUID, service: ActivityLogService = Depends(get_activity_log_service), _: User = Depends(get_current_user)):
-    return await service.get(id)
+async def get_log(
+    id: uuid.UUID,
+    service: ActivityLogService = Depends(get_activity_log_service),
+    current_user: User = Depends(get_current_user),
+    scoped_client_id: uuid.UUID | None = Depends(get_current_client_id),
+):
+    log = await service.get(id)
+    # SECURITY-4 (BAC-1): object-level check for client-scoped callers  -  they
+    # may read a log only if it belongs to their own user account.
+    if scoped_client_id is not None and log.user_id != current_user.id:
+        raise ForbiddenException("You do not have access to this resource.")
+    return log
 
 
 @router.post("", response_model=ActivityLogRead, status_code=status.HTTP_201_CREATED)
 async def create_log(data: ActivityLogCreate, db: AsyncSession = Depends(get_db), service: ActivityLogService = Depends(get_activity_log_service), _: User = Depends(get_current_user), _admin: str = Depends(require_roles("admin"))):
@@ -40,5 +63,5 @@ async def create_log(data: ActivityLogCreate, db: AsyncSession = Depends(get_db)
 
 @router.delete("/{id}", status_code=status.HTTP_204_NO_CONTENT)
 async def delete_log(id: uuid.UUID, db: AsyncSession = Depends(get_db), service: ActivityLogService = Depends(get_activity_log_service), _: User = Depends(get_current_user), _admin: str = Depends(require_roles("admin"))):
     await service.delete(id)
-    await db.commit()
+    await db.commit()
\ No newline at end of file

diff --git a/Backend/app/modules/activity_timeline/service.py b/Backend/app/modules/activity_timeline/service.py
--- a/Backend/app/modules/activity_timeline/service.py
+++ b/Backend/app/modules/activity_timeline/service.py
@@ -15,11 +15,19 @@ class ActivityLogService:
     def __init__(self, session: AsyncSession) -> None:
         self._session = session
         self._repo = ActivityLogRepository(session)
 
-    async def list_all(self, skip: int = 0, limit: int = 100, *, entity_type: str | None = None, entity_id: uuid.UUID | None = None) -> list[ActivityLog]:
-        if entity_type or entity_id:
-            return await self._repo.get_all_filtered(entity_type=entity_type, entity_id=entity_id, offset=skip, limit=limit)
+    async def list_all(
+        self, skip: int = 0, limit: int = 100,
+        *, entity_type: str | None = None, entity_id: uuid.UUID | None = None,
+        user_id: uuid.UUID | None = None,
+    ) -> list[ActivityLog]:
+        # SECURITY-4 (BAC-1): user_id scopes the query to a single caller.
+        if entity_type or entity_id or user_id:
+            return await self._repo.get_all_filtered(
+                entity_type=entity_type, entity_id=entity_id, user_id=user_id,
+                offset=skip, limit=limit,
+            )
         return await self._repo.get_all(offset=skip, limit=limit)
 
     async def get(self, id: uuid.UUID) -> ActivityLog:
         obj = await self._repo.get_by_id(id)
@@ -31,5 +39,5 @@ class ActivityLogService:
         return await self._repo.create_from_dict(data.model_dump())
 
     async def delete(self, id: uuid.UUID) -> None:
         if not await self._repo.delete(id):
-            raise NotFoundException("ActivityLog")
+            raise NotFoundException("ActivityLog")
\ No newline at end of file

diff --git a/Backend/app/modules/activity_timeline/repository.py b/Backend/app/modules/activity_timeline/repository.py
--- a/Backend/app/modules/activity_timeline/repository.py
+++ b/Backend/app/modules/activity_timeline/repository.py
@@ -16,10 +16,12 @@ class ActivityLogRepository(BaseRepository[ActivityLog]):
     def __init__(self, session: AsyncSession) -> None:
         super().__init__(session)
 
     async def get_all_filtered(self, *, entity_type: str | None = None, entity_id: uuid.UUID | None = None,
+                               user_id: uuid.UUID | None = None,
                                offset: int = 0, limit: int = 100) -> list[ActivityLog]:
         stmt = select(ActivityLog)
         if entity_type: stmt = stmt.where(ActivityLog.entity_type == entity_type)
         if entity_id: stmt = stmt.where(ActivityLog.entity_id == entity_id)
+        if user_id: stmt = stmt.where(ActivityLog.user_id == user_id)
         stmt = stmt.order_by(ActivityLog.created_at.desc()).offset(offset).limit(limit)
-        return (await self._db.execute(stmt)).scalars().all()
+        return (await self._db.execute(stmt)).scalars().all()
\ No newline at end of file

diff --git a/Backend/audit.py b/Backend/audit.py
--- a/Backend/audit.py
+++ b/Backend/audit.py
@@ -2,9 +2,10 @@ import asyncio
 from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
 from sqlalchemy import text
 import os
 
-DATABASE_URL = "postgresql+asyncpg://postgres.fhxkiprlcdwbgtaxlffk:REDACTED@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?prepared_statement_cache_size=0"
+# SECURITY (CRITICAL-2): secrets must come from the environment, never source.
+DATABASE_URL = os.environ["DATABASE_URL"]
 
 async def main():
     engine = create_async_engine(DATABASE_URL)
     async_session = async_sessionmaker(engine)
@@ -16,19 +17,5 @@ async def main():
         result_leads = await session.execute(text("SELECT count(*) FROM leads"))
         count_leads = result_leads.scalar()
         
         result_leads_new = await session.execute(text("SELECT count(*) FROM leads WHERE status = 'New'"))
-        count_leads_new = result_leads_new.scalar()
-        
-        result_leads_new_lower = await session.execute(text("SELECT count(*) FROM leads WHERE status = 'new'"))
-        count_leads_new_lower = result_leads_new_lower.scalar()
-        
-        result_contact = await session.execute(text("SELECT count(*) FROM contact_submissions"))
-        count_contact = result_contact.scalar()
-        
-        print(f"Consultation Requests: {count_cr}")
-        print(f"Contact Submissions: {count_contact}")
-        print(f"Leads Total: {count_leads}")
-        print(f"Leads (status='New'): {count_leads_new}")
-        print(f"Leads (status='new'): {count_leads_new_lower}")
-
-asyncio.run(main())
+        count_leads_new = result_leads_new.scalar()
\ No newline at end of file

diff --git a/Backend/test_api.py b/Backend/test_api.py
--- a/Backend/test_api.py
+++ b/Backend/test_api.py
@@ -5,11 +5,13 @@ from datetime import timedelta
 from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
 from sqlalchemy import text
 import jwt
 from datetime import datetime, timezone
+import os
 
-DATABASE_URL = "postgresql+asyncpg://postgres.fhxkiprlcdwbgtaxlffk:REDACTED@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?prepared_statement_cache_size=0"
-JWT_SECRET_KEY = "change-this-to-a-long-random-secret-in-production"
+# SECURITY (CRITICAL-2): secrets must come from the environment, never source.
+DATABASE_URL = os.environ["DATABASE_URL"]
+JWT_SECRET_KEY = os.environ["JWT_SECRET_KEY"]
 JWT_ALGORITHM = "HS256"
 
 def create_access_token(data: dict):
     to_encode = data.copy()
@@ -40,5 +42,5 @@ async def main():
             pass
             
     print("Token created:", token)
             
-asyncio.run(main())
+asyncio.run(main())
\ No newline at end of file

diff --git a/Backend/test_db.py b/Backend/test_db.py
--- a/Backend/test_db.py
+++ b/Backend/test_db.py
@@ -2,9 +2,10 @@ import asyncio
 from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
 from sqlalchemy import text
 import os
 
-DATABASE_URL = "postgresql+asyncpg://postgres.fhxkiprlcdwbgtaxlffk:REDACTED@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres?prepared_statement_cache_size=0"
+# SECURITY (CRITICAL-2): secrets must come from the environment, never source.
+DATABASE_URL = os.environ["DATABASE_URL"]
 
 async def main():
     engine = create_async_engine(DATABASE_URL)
     async_session = async_sessionmaker(engine)
@@ -23,5 +24,5 @@ async def main():
         result_roles = await session.execute(text("SELECT * FROM roles"))
         roles = result_roles.fetchall()
         print("Roles:", roles)
 
-asyncio.run(main())
+asyncio.run(main())
\ No newline at end of file

diff --git a/Backend/create_missing_tables.py b/Backend/create_missing_tables.py
--- a/Backend/create_missing_tables.py
+++ b/Backend/create_missing_tables.py
@@ -1,11 +1,12 @@
 """Create the 5 remaining tables that failed due to FK ordering."""
 import asyncio
 import os
 import sys
+from urllib.parse import urlparse
 
-sys.path.insert(0, r"C:\Users\DELL\Downloads\AMPLIVOPROJECT (2)\AMPLIVOPROJECT\AMPLIVOPROJECT")
-os.chdir(r"C:\Users\DELL\Downloads\AMPLIVOPROJECT (2)\AMPLIVOPROJECT\AMPLIVOPROJECT")
+sys.path.insert(0, os.environ.get("AMPLIVO_PATH", r"C:\Users\DELL\Downloads\AMPLIVOPROJECT (2)\AMPLIVOPROJECT\AMPLIVOPROJECT"))
+os.chdir(os.environ.get("AMPLIVO_PATH", r"C:\Users\DELL\Downloads\AMPLIVOPROJECT (2)\AMPLIVOPROJECT\AMPLIVOPROJECT"))
 os.environ["DB_SSL_MODE"] = "disable"
 
 from app.db.base import Base
 
@@ -51,8 +52,19 @@ from app.modules.websites.models import Website, WebsitePage, WebsiteMetric
 import asyncpg
 from sqlalchemy.schema import CreateTable
 from sqlalchemy import create_engine
 
+# SECURITY (CRITICAL-2): secrets must come from the environment, never source.
+# Derive the synchronous asyncpg connection URI from DATABASE_URL while
+# overriding the driver so no password is committed in this file.
+DATABASE_URL = os.environ["DATABASE_URL"]
+parsed = urlparse(DATABASE_URL)
+PG_HOST = parsed.hostname
+PG_PORT = parsed.port or 5432
+PG_DATABASE = parsed.path.lstrip("/") or "postgres"
+PG_USER = parsed.username
+PG_PASSWORD = parsed.password
+
 dummy_engine = create_engine("postgresql+psycopg2://", strategy="mock", executor=lambda *a, **kw: None)
 
 ALL_TABLES = set(Base.metadata.tables.keys())
 
@@ -65,9 +77,12 @@ RETRY_ORDER = [
     "website_metrics",
 ]
 
 async def run():
-    conn = await asyncpg.connect("postgresql://postgres:REDACTED@db.fhxkiprlcdwbgtaxlffk.supabase.co:5432/postgres", ssl="require")
+    conn = await asyncpg.connect(
+        host=PG_HOST, port=PG_PORT, database=PG_DATABASE,
+        user=PG_USER, password=PG_PASSWORD, ssl="require",
+    )
     
     for table_name in RETRY_ORDER:
         table = Base.metadata.tables[table_name]
         ddl = CreateTable(table).compile(dialect=dummy_engine.dialect)
@@ -99,5 +114,5 @@ async def run():
     print(f"\nFinal table count: {len(final)}")
     
     await conn.close()
 
-asyncio.run(run())
+asyncio.run(run())
\ No newline at end of file

diff --git a/frontend/src/store/authStore.ts b/frontend/src/store/authStore.ts
--- a/frontend/src/store/authStore.ts
+++ b/frontend/src/store/authStore.ts
@@ -1,6 +1,6 @@
 import { create } from 'zustand';
-import { persist } from 'zustand/middleware';
+import { createJSONStorage, persist } from 'zustand/middleware';
 
 export interface User {
   id: string;
   name: string;
@@ -21,9 +21,9 @@ interface AuthState {
   user: User | null;
   token: string | null;
   refreshToken: string | null;
   isAuthenticated: boolean;
-  // Zustand's persist middleware rehydrates from localStorage
+  // Zustand's persist middleware rehydrates from sessionStorage
   // asynchronously after the initial render, so isAuthenticated is always
   // false for one tick on a hard page load. Consumers that gate a redirect
   // on isAuthenticated (e.g. ProtectedRoute) must wait for hasHydrated -
   // otherwise every hard navigation/refresh has a race that can bounce a
@@ -52,20 +52,28 @@ export const useAuthStore = create<AuthState>()(
         // project ids, hrStore's cached state) must not survive a logout -
         // otherwise the next person to use this browser/device inherits the
         // previous user's leftover UI state.
         if (typeof window !== 'undefined') {
+          window.sessionStorage.removeItem('auth-storage');
           window.localStorage.removeItem('amplivo-crm-store');
           window.localStorage.removeItem('amplivo-hr-storage');
+          // SECURITY (MED-2): also clear the legacy localStorage copy if any
+          // user ran an older build that persisted the token there.
+          window.localStorage.removeItem('auth-storage');
         }
       },
       setToken: (token) => set({ token }),
       setHasHydrated: (hasHydrated) => set({ hasHydrated }),
       updateUser: (patch) => set((state) => (state.user ? { user: { ...state.user, ...patch } } : state)),
     }),
     {
       name: 'auth-storage',
+      // SECURITY (MED-2): use sessionStorage so the JWT never survives in
+      // localStorage (which any XSS can read and which persists across tabs/
+      // browser restarts). sessionStorage is per-tab and cleared on close.
+      storage: createJSONStorage(() => sessionStorage),
       onRehydrateStorage: () => (state) => {
         state?.setHasHydrated(true);
       },
     }
   )
-);
+);
\ No newline at end of file
